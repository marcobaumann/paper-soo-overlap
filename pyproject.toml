[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "soo-empathy"
version = "0.1.0"
description = "Self-other-overlap / empathy research"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
